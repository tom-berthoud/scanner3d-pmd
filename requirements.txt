numpy>=1.24
opencv-python>=4.8
scipy>=1.11
# Official Open3D wheels are limited by Python/platform tags. On Raspberry Pi
# aarch64 with Python 3.13, use the ARM wheel package that still imports as open3d.
open3d-unofficial-arm>=0.19.0.post9; platform_machine == "aarch64"
open3d>=0.18; platform_machine != "aarch64" and python_version < "3.13"
flask>=3.0
pyyaml>=6.0
pytest>=7.4
